{
  "name": "openclaw-wa-archive",
  "version": "0.1.0",
  "description": "OpenClaw plugin that archives WhatsApp messages with full-text and semantic search",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "sqlite-vec": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0",
    "@types/better-sqlite3": "^7.6.0"
  }
}
